{
  "$schema": "https://railway.app/railway.schema.json",
  "builds": [
    {
      "builder": "NIXPACKS",
      "buildCommand": "composer install --no-dev --optimize-autoloader && npm ci && npm run build && rm -rf public/storage && cp -r storage/app/public public/storage && php artisan config:cache && php artisan route:cache && php artisan view:cache"
    }
  ],
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}"
  }
}
